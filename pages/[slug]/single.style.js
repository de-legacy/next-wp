import css from 'styled-jsx/css';
import values from 'styles/values.style';

export default css` 
  .single-title {
    margin: 0 0 3rem 0;
    font-size: ${values.SINGLE_TITLE_SIZE};
    line-height: 4.75rem;
  }

  .single {
    margin: 0 30% 0 0;
  }
`;